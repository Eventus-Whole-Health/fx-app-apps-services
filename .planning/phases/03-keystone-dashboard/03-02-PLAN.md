---
phase: 03-keystone-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - keystone-platform/frontend/src/views/scheduler/components/ExecutionHistoryDrawer.tsx
  - keystone-platform/frontend/src/views/scheduler/components/RunDetailPanel.tsx
  - keystone-platform/frontend/src/views/scheduler/components/JsonTreeViewer.tsx
  - keystone-platform/frontend/src/views/scheduler/SchedulerDashboard.tsx
  - keystone-platform/frontend/src/views/scheduler/components/ServiceSubGrid.tsx
autonomous: true
requirements:
  - DASH-03

must_haves:
  truths:
    - "Clicking a service in the sub-grid opens a right-side drawer showing execution history"
    - "Execution history shows last 10 runs by default with status badge, start time, duration, and error message"
    - "Clicking a run expands it to reveal full request/response payloads as formatted JSON"
    - "A 'Load more' button fetches older history pages"
    - "Drawer closes cleanly and re-opens for different services"
  artifacts:
    - path: "keystone-platform/frontend/src/views/scheduler/components/ExecutionHistoryDrawer.tsx"
      provides: "Right-side slide panel with execution history list"
      min_lines: 80
    - path: "keystone-platform/frontend/src/views/scheduler/components/RunDetailPanel.tsx"
      provides: "Expandable run detail with timing and error info"
      min_lines: 40
    - path: "keystone-platform/frontend/src/views/scheduler/components/JsonTreeViewer.tsx"
      provides: "Formatted, syntax-highlighted, collapsible JSON display"
      min_lines: 30
  key_links:
    - from: "keystone-platform/frontend/src/views/scheduler/components/ServiceSubGrid.tsx"
      to: "keystone-platform/frontend/src/views/scheduler/components/ExecutionHistoryDrawer.tsx"
      via: "onServiceClick callback opens drawer"
      pattern: "onServiceClick|setSelectedService"
    - from: "keystone-platform/frontend/src/views/scheduler/components/ExecutionHistoryDrawer.tsx"
      to: "keystone-platform/frontend/src/services/scheduler-api.ts"
      via: "useServiceHistory SWR hook"
      pattern: "useServiceHistory"
---

<objective>
Build the execution history drill-down view: a right-side drawer that opens when clicking a service in the sub-grid, showing past runs with timing, status, error details, and expandable request/response payloads displayed as formatted JSON.

Purpose: Delivers DASH-03 (execution history). Developers can investigate any service's recent behavior without touching SQL, seeing exactly what happened on each run including full request/response data.

Output: Functional drawer component integrated with the sub-grid click handler, loading history from the scheduler API.
</objective>

<execution_context>
@/Users/jgilpatrick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jgilpatrick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-keystone-dashboard/03-CONTEXT.md
@.planning/phases/03-keystone-dashboard/03-01-SUMMARY.md

# UI components available:
@keystone-platform/frontend/src/components/ui/sheet.tsx
@keystone-platform/frontend/src/components/ui/badge.tsx
@keystone-platform/frontend/src/components/ui/card.tsx
@keystone-platform/frontend/src/components/ui/scroll-area.tsx
@keystone-platform/frontend/src/components/ui/button.tsx
@keystone-platform/frontend/src/components/ui/collapsible.tsx
@keystone-platform/frontend/src/components/ui/skeleton.tsx

# Service layer from Plan 03-01:
@keystone-platform/frontend/src/services/scheduler-api.ts
@keystone-platform/frontend/src/types/scheduler.ts

# Existing components from Plan 03-01:
@keystone-platform/frontend/src/views/scheduler/SchedulerDashboard.tsx
@keystone-platform/frontend/src/views/scheduler/components/ServiceSubGrid.tsx
@keystone-platform/frontend/src/views/scheduler/components/HealthBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON tree viewer and run detail panel components</name>
  <files>
    keystone-platform/frontend/src/views/scheduler/components/JsonTreeViewer.tsx
    keystone-platform/frontend/src/views/scheduler/components/RunDetailPanel.tsx
  </files>
  <action>
    **JsonTreeViewer** — Formatted, syntax-highlighted, collapsible JSON display:
    - Accept `data: string | null` prop (raw JSON string from API)
    - Parse JSON and render as a tree with:
      - Indentation (2-space) with color-coded values: strings in green, numbers in blue, booleans in purple, null in gray
      - Object keys in standard text color
      - Collapsible nested objects/arrays: click `{...}` or `[...]` to expand/collapse
      - Use `useState` for collapsed node tracking
    - If data is null or empty, show "No data" in muted text
    - If JSON parse fails, show raw string in monospace pre block
    - Wrap in a `div` with `font-mono text-xs` styling and `max-h-80 overflow-auto` scroll container
    - Use Tailwind classes for syntax colors — no external library needed for this scope

    **RunDetailPanel** — Expandable run detail within history list:
    - Accept `entry: ExecutionHistoryEntry` and `isExpanded: boolean` and `onToggle: () => void` props
    - **Summary row** (always visible):
      - Status badge using HealthBadge-style coloring (success=green, failed/error=red, timeout=amber, pending=gray)
      - Start time formatted as "MMM dd, HH:mm:ss" (use date-fns `format`)
      - Duration formatted as human-readable: "<1s", "2.3s", "1m 15s", "15m 30s"
      - Error message (truncated to 80 chars with tooltip for full text) — only shown if status is not success
      - ChevronRight icon that rotates 90deg when expanded (follow FunctionAppGrid pattern)
    - **Expanded detail** (shown when isExpanded=true):
      - Use `AnimatePresence` + `motion.div` for smooth expand/collapse
      - Full timing info: Started at, Ended at, Duration (all formatted)
      - Error message (full, untruncated) in a red-tinted card if present
      - "Request Payload" section with JsonTreeViewer if request_data exists
      - "Response Payload" section with JsonTreeViewer if response_data exists
      - Each payload section is collapsible via Collapsible component from shadcn
    - Summary row is clickable — calls `onToggle()` on click
  </action>
  <verify>
    - JsonTreeViewer handles: valid JSON, invalid JSON (fallback to raw), null data, empty string
    - RunDetailPanel renders status badge, time, duration in summary row
    - RunDetailPanel expands to show payloads when clicked
    - TypeScript compilation passes: `cd keystone-platform/frontend && npx tsc --noEmit --pretty 2>&1 | head -30`
  </verify>
  <done>
    JsonTreeViewer renders formatted, syntax-highlighted, collapsible JSON trees. RunDetailPanel shows summary row with status/time/duration and expands to reveal full error details and request/response payloads.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create execution history drawer and integrate with dashboard</name>
  <files>
    keystone-platform/frontend/src/views/scheduler/components/ExecutionHistoryDrawer.tsx
    keystone-platform/frontend/src/views/scheduler/SchedulerDashboard.tsx
    keystone-platform/frontend/src/views/scheduler/components/ServiceSubGrid.tsx
  </files>
  <action>
    **ExecutionHistoryDrawer** — Right-side slide panel for execution history:
    - Use Sheet/SheetContent/SheetHeader/SheetTitle from `@/components/ui/sheet`
    - Accept props:
      - `service: SchedulerService | null` (null = closed)
      - `open: boolean`
      - `onOpenChange: (open: boolean) => void`
    - SheetContent side="right" with custom width class: `sm:max-w-lg` (override the default sm:max-w-sm to give more room for payload viewing — Claude's discretion on drawer width)
    - Sheet header shows: service name as title, function app as subtitle in muted text, HealthBadge for current health status
    - Body uses ScrollArea component for scrollable history list
    - Calls `useServiceHistory(service?.id, page, 10)` — page_size=10 per user decision (last 10 runs by default)
    - Loading state: 3-4 Skeleton components matching RunDetailPanel height
    - Empty state: "No execution history found" centered text with Clock icon
    - Renders list of `RunDetailPanel` components, one per history entry
    - Track expanded run via `expandedRunId: number | null` state — only one run expanded at a time
    - **"Load more" button** at bottom of list:
      - Only visible when `page < total_pages` from the API response
      - Clicking increments page, which triggers `useServiceHistory` to refetch
      - Button text: "Load more runs" with ChevronDown icon
      - Show Spinner (RefreshCw with animate-spin) while loading next page
      - Accumulate results: maintain a local state array that appends new pages' entries to existing ones (since SWR would replace, store accumulated entries in useState and append on new page load)

    **SchedulerDashboard updates**:
    - Add `selectedService` state: `useState<SchedulerService | null>(null)`
    - Add `drawerOpen` state: `useState(false)`
    - Pass `onServiceClick` handler to FunctionAppCard → ServiceSubGrid that sets selectedService and opens drawer
    - Render `<ExecutionHistoryDrawer>` at bottom of page component, passing selectedService, drawerOpen, onOpenChange
    - When drawer closes (onOpenChange(false)), clear selectedService after animation completes (small timeout or just clear immediately)

    **ServiceSubGrid updates**:
    - Add `onServiceClick: (service: SchedulerService) => void` prop
    - Attach onClick handler to each service row that calls `onServiceClick(service)`
    - Add `cursor-pointer` class to rows and `hover:bg-muted/60` for click affordance
    - Stop event propagation on any future action buttons in the row (prepare for Plan 03-03)
  </action>
  <verify>
    - Clicking a service row opens the right-side drawer with that service's name in header
    - Drawer loads and displays execution history entries with status badges and timing
    - Clicking a run entry expands it to show payloads
    - "Load more" button appears when there are additional pages and loads them
    - Closing drawer clears selection
    - TypeScript compilation: `cd keystone-platform/frontend && npx tsc --noEmit --pretty 2>&1 | head -30`
  </verify>
  <done>
    Clicking any service in the sub-grid opens a right-side drawer showing execution history. History shows last 10 runs with status badges, timing, and error messages. Clicking a run expands it to reveal request/response payloads as formatted JSON trees. "Load more" button paginate through older runs.
  </done>
</task>

</tasks>

<verification>
1. Sub-grid row click opens right-side drawer with service name in header
2. Drawer loads history from `useServiceHistory` SWR hook
3. History entries show: status badge, formatted start time, human-readable duration, truncated error
4. Clicking a history entry expands to show full error and JSON payloads
5. JSON payloads are syntax-highlighted with collapsible nested objects
6. "Load more" button fetches additional pages of history
7. Drawer closes cleanly and can be re-opened for different services
8. All new files compile without TypeScript errors
</verification>

<success_criteria>
- Developer can click any service → see its last 10 execution runs in a drawer
- Each run shows timing and status at a glance
- Failed runs show full error details when expanded
- Request/response payloads are viewable as formatted JSON
- Pagination works via "Load more" for investigating older runs
</success_criteria>

<output>
After completion, create `.planning/phases/03-keystone-dashboard/03-02-SUMMARY.md`
</output>
